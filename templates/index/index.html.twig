<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome to TheBlog{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/homepage.css') }}">
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_index') }}">
                    {# <img src="{{ asset('images/logo.png') }}" alt="TheBlog Logo"> #}
                    TheBlog
                </a>
                <ul class="nav-links">
                    <li><a href="{{ path('blog_new') }}">Create New Post</a></li>
                    {% if app.user %}
                        <li><a href="{{ path('app_logout') }}">Logout</a></li>
                    {% else %}
                        <li><a href="{{ path('app_login') }}">Login</a></li>
                    {% endif %}
                </ul>
            </div>
        </nav>
    </header>
    
    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <footer>
        <p>&copy; {{ 'now'|date('Y') }} TheBlog. All rights reserved.</p>
    </footer>
</body>
</html>
